# Repository location

Canonical clone/worktree for this project:

- `/root/git/PhonyC`

Reference capture logs may live under a workspace folder (e.g. `/root/PhonyC`) and are gitignored; do not commit them.
